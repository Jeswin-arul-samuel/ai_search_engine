# ai_search_engine
This is a Search Engine that uses tools like wikipedia, arxiv and google search to fetch information to the user query and then uses LLM's to summarize the information based on the user's question
